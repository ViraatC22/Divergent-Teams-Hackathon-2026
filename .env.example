# Clerk (frontend auth)
VITE_CLERK_PUBLISHABLE_KEY=pk_test_...

# Clerk (server-side only — NEVER expose to frontend)
CLERK_SECRET_KEY=sk_test_...
# Register webhook at: Clerk Dashboard → Webhooks → Add Endpoint → <ngrok>/webhooks/clerk
# Subscribe to: user.created, user.updated, session.created
CLERK_WEBHOOK_SECRET=whsec_...

# Supabase (frontend client)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=eyJ...

# Supabase (server-side only — NEVER expose to frontend)
SUPABASE_SERVICE_ROLE_KEY=eyJ...
